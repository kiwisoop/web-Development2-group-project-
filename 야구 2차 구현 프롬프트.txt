I want to implement the second step of the BASEBALL module.

Use the existing first-step BASEBALL implementation as the base.
Keep React + Spring Boot.
Keep the backend package as com.team.sportsanalysis.
Do not use Thymeleaf.
Do not add Spring Security.
Do not break existing SOCCER, VOLLEYBALL, BASKETBALL, ESPORTS, or the Baseball schedule page.

Second step goal:
Add a Baseball game detail page with MLB live game detail, inning scoreboard, R/H/E, and event timeline.

Backend:
1. Add GET /api/mlb/game/{gamePk}
2. Use MLB live feed endpoint:
   https://statsapi.mlb.com/api/v1/game/{gamePk}/feed/live
3. Create DTOs if needed:
   - MlbGameDetail
   - MlbInningScore
   - MlbGameEvent
4. Return simplified JSON only.
5. Include:
   - gamePk
   - gameDate
   - status
   - venue
   - homeTeam
   - awayTeam
   - homeScore
   - awayScore
   - currentInning
   - inningHalf
   - awayHits
   - homeHits
   - awayErrors
   - homeErrors
   - inning scores
   - major play events
6. For events, parse scoring plays or recent major plays from liveData.plays.allPlays.
7. Keep parsing defensive. If a field is missing, return null or "-" safely.

Frontend:
1. Create BaseballDetailPage.jsx.
2. Add route:
   /baseball/:gamePk
3. Add Detail button to each MLB game card in BaseballPage.jsx.
4. Create BaseballScoreboard.jsx.
5. Create BaseballEventTimeline.jsx.
6. BaseballDetailPage should show:
   - teams
   - score
   - status
   - venue
   - current inning
   - inning scoreboard
   - R / H / E
   - event timeline
   - manual refresh button
7. Reuse existing CSS classes where possible.
8. Keep the UI consistent with the current dark dashboard style.

Do not implement fan chat yet.
Do not implement strike zone yet.
Do not implement hit-risk analysis yet.
Do not connect to DB yet.
This step is only MLB game detail + inning scoreboard + event timeline.

After implementation, show only:
1. changed files
2. run commands
3. test URLs